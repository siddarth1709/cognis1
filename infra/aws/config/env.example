# Copy values into your shell or deployment workflow as needed.
AWS_REGION=ap-south-1
COGNIS_MODEL_ID=anthropic.claude-sonnet-4-6
COGNIS_AUTONOMY_THRESHOLD=0.75
ENABLE_CLOUDFRONT=false
# Generate a unique server-only key and pass it as the CognisApiKey deployment parameter.
COGNIS_API_KEY=
