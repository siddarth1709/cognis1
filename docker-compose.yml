services:
  cognis-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cognis-web
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - apps/web/.env.local
    environment:
      - PORT=3000
      - HOSTNAME=0.0.0.0
